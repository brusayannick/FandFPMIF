"""Flows & Funds — FastAPI backend.

The platform package. Modules live outside this tree (in `modules/`); their
SDK lives in `flows_funds.sdk` (the sibling workspace member).
"""

__version__ = "0.1.0"
