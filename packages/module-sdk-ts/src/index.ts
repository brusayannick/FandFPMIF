/**
 * Public TS SDK for module frontends. Empty for v1 — module bundles will
 * import from here once the dynamic loader (frontend.panel from a module's
 * .dist/) lands. Until then this file exists so the workspace resolves.
 */

export const SDK_VERSION = "0.1.0";
